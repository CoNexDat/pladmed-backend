version: '3'
services:
  server:
    container_name: server
    build: ./
    entrypoint: flask run --host 0.0.0.0
    environment:
      - PYTHONUNBUFFERED=1
      - FLASK_APP=src/main.py
    volumes:
      - ./src:/home/src
    ports:
      - 5000:5000
